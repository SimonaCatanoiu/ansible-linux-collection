# Ansible vault password file
# Commands (DO NOT LET THIS FILE IN PLAINTEXT): 
# ansible-vault encrypt secrets.yaml -> before running the playbook
# ansible-vault decrypt secrets.yaml -> when wanting to change the passwords
passwords:
  old_admin_password: <input>
  old_root_password: <input>
  new_admin_password: <input>
  new_root_password: <input>